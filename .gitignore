# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
.env
.venv
venv/
ENV/

# Database
backend/agrinova.db

# PDFs and debug logs
backend/*.pdf
backend/*.txt
backend/test_*.py

# Node/React
node_modules/
dist/
.DS_Store
*.local
.vscode/

# ML Models
# model.pkl is usually kept if it's the principal artifact, 
# but big files might be excluded if we prefer training on-site.
# Given the user's request to "remove unnecessary files", I'll keep model.pkl 
# since it's the result of training, but exclude the "test" scripts.
